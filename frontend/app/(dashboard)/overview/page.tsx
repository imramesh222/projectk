import { default as OverviewInfo } from "@/components/pages/overview/overview-info/OverviewInfo";

const page = async () => {
  return (
    <div>
      <OverviewInfo />
    </div>
  );
};

export default page;
