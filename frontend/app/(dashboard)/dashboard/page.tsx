import DashboardInfo from "@/components/pages/dashboard/dashboard-info/DashboardInfo";

const page = async () => {
  return (
    <div>
      <DashboardInfo />
    </div>
  );
};

export default page;
